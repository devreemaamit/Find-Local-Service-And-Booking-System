name: Keep Render App Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to keep alive
        run: |
          curl https://loan-app-wjbq.onrender.com/api/server/start
